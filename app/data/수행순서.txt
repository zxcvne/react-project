next.js <-> DB 연결

- 설치 라이브러리 
npm i mysql2 => DB

- DB 구성 완료 => 파일 참조.

디렉토리 구조

1.  폴더 구성
    api/voca 
    components > layout > Header.tsx / Footer.tsx / LoginHeader / Ranking
    components > voca > VocaList.tsx / 
    components > login > VocaList.tsx / 
    components > loginUser > VocaList.tsx / 
    login
    data > data.ts (sample data)
    type > vocaType.ts (vocaType 정의)
    api > DB 연결에 필요한 서버 라인 함수
    board > 라우트 연결 page
    lib > DB 설정 구문